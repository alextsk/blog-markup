mixin toggle(initial)
  - var id = Math.floor(Math.random() * 10000 + '')
  .toggle
    input(type='checkbox', id=id, checked=initial).toggle__actual
    label.toggle__label(for=id)
      .toggle__circle
      